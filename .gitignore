# Ignore sensitive or user-specific files
.DS_Store
*.log
*.tmp

# Temp files generated per run — not shared state
.slack-post-data.json
.tracker-block.tsv

# Local machine state — each user gets their own on first run
.last-poc-check

# Note: epic-config.json and release-schedule.json are included
# These contain GUS epic IDs and release dates which are safe to commit
